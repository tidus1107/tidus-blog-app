%p #{@user.email} さん
%p 以下のユーザにフォローされました。

= link_to 'プロフィールを確認する', account_url(@follower)

-# _pathと_urlの違い

-# users_path => '/users'
-# users_url => 'http://localhost:3000/users'

-# パスの場合はサイトの今いる場所にURLを足すイメージ
-# urlの場合はメールなどウェブページ以外から飛んで来る場合に使用する
